export enum Event {
  CLICK = "click",
}

export enum ServerEvent {
  USER_JOIN = "USER_JOIN",
  USER_LEAVE = "USER_LEAVE",
  USER_DATA = "USER_DATA",
  USER_READY = "USER_READY",
  USER_EXIT = "USER_EXIT",

  DISCONNECT_USER = "DISCONNECT_USER",
}
